import { ClusterFormSchema } from '@/schemas/cluster.schema'

export const EMPTY_VALUES: ClusterFormSchema = {
    name: '',
    zone_id: '',
    identifier: '',
}
export const LIST_KEY = 'cluster-list'
